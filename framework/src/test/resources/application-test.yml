spring:
  datasource:
    url: jdbc:h2:mem:test;DB_CLOSE_DELAY=-1;MODE=Oracle;
    driverClassName: org.h2.Driver
    username: test
    password: test
    schema: classpath:sql/data.sql

apache:
  rocketmq:
    consumer:
      consumerGroup: Consumer # 消费者的组名
    producer:
      producerGroup: Producer # 生产者的组名
    namesrvAddr: 10.1.11.111:32426 # NameServer地址